# Docker

Docker images for rust-ceramic are available from our AWS container registry.

Currently, the latest release is `${version}`.
```
docker pull public.ecr.aws/r5b3e0r5/3box/ceramic-one:${version}
```
